# Optional example. Keep the OmniRoute API private unless you intentionally publish it.
# router.example.com { reverse_proxy omniroute:20128 }
# hermes.example.com { reverse_proxy hermes:9119 }
# chat.example.com { reverse_proxy open-webui:8080 }
