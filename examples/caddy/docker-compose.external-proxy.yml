# Optional HTTP-only Caddy origin for deployments with a separate TLS reverse-proxy VM.
# Use together with the repository's normal docker-compose.yml.
#
# Example:
# CADDY_HTTP_BIND_IP=10.20.0.20 CADDY_HTTP_PORT=8088 \
#   docker compose -f docker-compose.yml \
#   -f examples/caddy/docker-compose.external-proxy.yml \
#   --profile caddy-http up -d caddy-http

services:
  caddy-http:
    profiles: ["caddy-http"]
    image: ${CADDY_IMAGE:-caddy:2-alpine}
    container_name: hermes-stack-caddy-http
    restart: unless-stopped
    ports:
      - "${CADDY_HTTP_BIND_IP:-127.0.0.1}:${CADDY_HTTP_PORT:-8088}:80"
    volumes:
      - ./data/caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - ./data/caddy/data:/data
      - ./data/caddy/config:/config
    networks: [agent-net]
