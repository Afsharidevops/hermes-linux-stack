# Hermes Smart Router v0.5.9 Release Notes

**Release date:** 2026-08-13

## Visual Flow Connections

- Shared port-aware graph engine across Workflow, Agent, Router Pipeline, and Knowledge Pipeline studios.
- Drag output handles to compatible input handles with live previews and target validation.
- Named branch outputs including condition true/false, approval approved/rejected/timeout, and router health/classifier paths.
- Edge selection, deletion, reconnection, quick-add, undo/redo, pan, zoom, and fit-to-view.
- Port-aware persistence with v0.5.8 graph normalization.
- Agent graph companion persistence and Router graph/runtime transition integration.

## Reliability

- Fixed direct Users, Policies, and Plugins create APIs returning HTTP 500 after commit due to detached/expired ORM rows during audit logging.

## Security

Visual graph structure does not grant privileged execution authority. Existing execution policy, approval, signing, identity, Docker/SSH separation, and Execution Broker boundaries remain authoritative.

## Acceptance

Automated graph, API, persistence, regression, Compose, manifest, executable-mode, and full Smart Router smoke suites passed during candidate preparation.

The release owner explicitly requested early finalization and waived the remaining manual browser light/dark rendering and mouse/trackpad interaction gate. This waiver is recorded as a waiver, not as a passing manual test.
