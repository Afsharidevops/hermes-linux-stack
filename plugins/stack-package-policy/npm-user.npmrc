; Intentionally empty stack-owned npm "user" config.
; npm 10.11+ refuses to load the same path as both user and global config, so
; this file and npm-global.npmrc must remain distinct. Security-relevant
; settings are passed as highest-precedence CLI arguments by the broker.
